namespace Core.Metadata;

public enum LanguageEnum
{
    EN,
    FA,
    HY,
    TR,
    RU,
    ZH,
    JA,
    AR,
    KU
}

//public static Language Of(string name)
//{
//    foreach (Language p)

//        if (p.GetDisplayName().Equals(name,StringComparison.OrdinalIgnoreCase)) return p;

//    return EN;
//}